# 儿童象棋项目上下文

最后更新：2026-07-29

## 项目是什么

这是给用户自己约 5 岁孩子使用的中国象棋网页应用，不是商业产品、专业引擎或系统课程。

最重要的设计原则：

> 电脑不是“最会下棋”，而是“最适合陪 5 岁孩子学象棋”的伙伴。

目标是让孩子喜欢下棋，而不是只喜欢赢棋。电脑必须认真遵守规则、有一定逻辑，也要保留儿童棋手会有的不完美，让孩子觉得“我努力一点，下次就能赢”。

唯一目标用户已经知道所有棋子的基本规则，喜欢互动下棋，不喜欢固定教学视频，可以无限悔棋。

## 产品边界

第一版保持纯前端和简单界面，只需要棋盘、悔棋、轻提示、重新开始和难度选择。

明确不做：登录、联网对战、好友、排行榜、广告、付费、商城、账号、数据库、专业引擎、庞大开局库、视频课程和复杂动画。

所有棋局仅保存在当前设备的 `localStorage`。孩子使用的公开免登录地址是：

- https://tangyiwen11.github.io/kids-xiangqi/

另有一个仅限所有者访问的 Sites 部署：

- https://kids-xiangqi-companion.yiwen-tang794974.chatgpt.site

不要把私有 Sites 地址当作给 iPad 的主要链接，避免再次产生登录困惑。

## 当前已经实现

- 完整中国象棋合法规则
- 马腿、象眼、炮架、将帅照面、将军、应将和将死
- 点击棋子、点击目标位置移动
- 无限悔棋、轻提示、重新开始
- 自动保存与恢复上一盘
- iPad、iPhone、Android、Mac 和 Windows 响应式界面
- Safari 添加到主屏幕和离线缓存
- 重要提示语音：将军、被将军、非法走法、将死
- 轻量开局习惯和文字说明

语音要克制。开局介绍和普通教学提示只显示文字，不朗读；语音主要保留给“将军、被将军、这里不能走”等重要信息。

## 当前难度定义

难度是固定的，不会根据孩子输赢自动改变。

### 一级 · 轻松

等于 2026-07-29 调整前的二级。会看孩子下一步最直接的吃子，会避免部分明显送子，但有较大随机性，会从约 4 个合理走法中选择。

### 二级 · 刚好

等于调整前的三级。比一级更重视眼前危险，随机性更小，会从约 3 个较合理走法中选择。

### 三级 · 挑战

会计算：

1. 电脑当前走法
2. 孩子较强的回应
3. 电脑下一步的继续处理

当前使用有限分支的两层前瞻：先从较合理的根走法中搜索，后续每层保留重点走法。它能理解简单的连续吃子、将军和送子后果，但仍从少量优秀候选中随机选择，不应变成每步绝对最优的专业引擎。

已保存的旧难度会通过 `difficultyVersion: 2` 自动迁移：旧二级变为新一级，旧三级变为新二级。

## 开局系统

电脑开局会从以下轻量布阵习惯中选择：

- 屏风马
- 中炮布阵
- 起马布阵
- 飞象布阵

它会参考孩子第一步，例如孩子走中炮时，电脑更可能选择屏风马或中炮回应。开局习惯是评分偏好，不是死背棋谱；遇到将军、吃大子或必须处理的危险时，应优先处理真实局面。

开局前两轮可以在右侧短句说明“小木正在尝试什么布阵”，不要长篇教学，也不要每步弹窗。

## 代码结构

- `app/xiangqi.ts`：唯一主要规则和电脑走棋源代码
- `app/page.tsx`：React 版本的棋局状态、交互、提示、语音和保存
- `app/globals.css`：页面和移动端样式
- `docs/`：GitHub Pages 使用的无服务器静态版本
- `docs/game.js`：静态版交互，与 `app/page.tsx` 的行为保持一致
- `docs/engine.js`：由 `app/xiangqi.ts` 生成，不要手工维护不同的算法
- `public/sw.js`、`docs/sw.js`：离线缓存；发布影响网页行为的更新时递增缓存版本
- `.openai/hosting.json`：现有 Sites 项目标识，不要创建第二个站点

修改 `app/xiangqi.ts` 后，重新生成静态引擎：

```bash
./node_modules/.bin/esbuild app/xiangqi.ts --bundle --format=iife --global-name=Xiangqi --outfile=docs/engine.js
```

修改 React 交互时，同时检查 `docs/game.js` 是否需要同步，因为孩子主要使用 GitHub Pages 的 `docs/` 版本。

## 验证和发布

至少执行：

```bash
npm run build
node --check docs/game.js
node --test tests/rendered-html.test.mjs
git diff --check
```

电脑算法调整后，还应使用随机合法局面验证：

- 电脑返回的每一步都在 `getLegalMoves` 中
- 被将军时一定应将
- 走完后自己的将不能仍被攻击
- 不出现明显性能回退，尤其关注 iPad

GitHub 仓库：

- `https://github.com/tangyiwen11/kids-xiangqi`
- 本地远程名为 `github`
- `main` 分支的 `docs/` 目录用于 GitHub Pages

运行时改动需要同时更新 GitHub Pages 和现有 Sites 部署。纯文档改动只需提交并推送 GitHub，不必重新部署网页。

## 后续优化方向

先让孩子实际试玩，再根据真实表现调整。优先观察：

- 各难度的胜负体验是否有明显区别
- 三级是否真的能发现简单的连续战术
- 哪些局面电脑仍会无意义送车、炮或马
- 开局提示是否帮助孩子形成棋感，而不是变成背术语

可能的后续版本包括儿童残局、单棋子训练、轻量 AI 老师和仅供家长查看的统计，但在用户明确决定前不要提前扩展。

